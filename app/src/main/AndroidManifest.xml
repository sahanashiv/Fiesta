<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fiesta">

    <application
        android:allowBackup="true"
        android:icon="@drawable/college_image_icon_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoTheme">
        <activity android:name=".EventDisplayActivity"></activity>
        <activity android:name=".FestUpdateActivity" />
        <activity android:name=".AddEvent">

        </activity>
        <activity android:name=".CollegeFragement" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>